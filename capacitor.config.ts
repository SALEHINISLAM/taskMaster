import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.professionalupdate.taskmaster',
  appName: 'TaskMaster',
  webDir: 'dist'
};

export default config;
